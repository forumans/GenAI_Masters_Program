# HR Assistant — top-level requirements
# Install sub-project dependencies directly from their own requirements files:
#
#   Backend (Python/FastAPI):
#     pip install -r hr_assistant_api/requirements.txt
#
#   Frontend (Node/React) — install via npm, not pip:
#     cd hr_assistant_web && npm install

# Optional: install the API package in editable mode for development
-r hr_assistant_api/requirements.txt
